# BACKLOG

One file, not split, because items cross stage boundaries.

Each item is tagged with the stage it arose in and a severity.

**Severity:** `blocker` stops a stage passing, `should` is worth doing before handover, `nice` is phase two.

Format: `- [stage] [severity] description`

---

- [0] [should] Local folder is spelled `CMS Open Payments Dashbaord`. Rename it once the desktop folder connection can be re-added.
- [0] [should] Decide how the delivered pbix is distributed. A pbix built against the full five years will be far too large for git, so it is either built against the sample data or published as a release asset rather than committed. Settle at stage 8.
